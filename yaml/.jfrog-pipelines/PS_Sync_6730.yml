

pipelines:
  - name: Ps_variable_resolved_67301
    steps:
      - name: Ps_variable_resolved_67301
        type: Bash
        configuration:
          integrations:
            - name: genericExport
            - name: s_artifactory
          environmentVariables:
            env1: {{gitBranch}}
            env2: {{.jfrog-pipelines.sourceRepository}}
          
        execution:
          onExecute:
            - echo ${int_genericExport_genericExport}
            - echo ${int_s_artifactory_user}
            - echo ${genericExport}
            - echo {{.jfrog-pipelines.sourceRepository}}
            
            
