resources:
  #GIT RESOURCES
  - name: Publish_Template_10390_002
    type: GitRepo
    configuration:
      path: nitind123/newTemplates
      gitProvider: s_gitHub
      branches:
        include: master

pipelines:
  - name: Publish_Template_10390_0021
    steps:
      - name: S_flow
        type: Bash
        configuration:
          environmentVariables:
            env1: "test"
        execution:
          onExecute:
            - echo 'data'
