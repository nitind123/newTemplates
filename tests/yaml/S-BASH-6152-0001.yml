pipelines:
  - name: S_Bash_6152_0001
    steps:
      - name: S_Bash_6152_0001
        type: Bash
        configuration:
          nodePool: u18k8sdynamic_6080         
        execution:
          onExecute:
            - mkdir -p test && mount /dev/sdb test
